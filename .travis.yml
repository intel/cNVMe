language: cpp
compiler:
  - g++
notifications:
  email:
    on_success: never
    on_failure: always
script:
  - cd cNVMe && ./build.sh && ./a.out
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5